/// @description Vente Clope — Index 0 — Gratuit — 10 €/s
/// Parent : obj_Business

event_inherited(); // Récupère le Create du parent
biz_index = 0;

// Clope est déjà débloqué par le GameManager (prix = 0)
// Rien de plus à faire ici
