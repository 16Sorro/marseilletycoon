/// @description Scooter - Init

spd = 4;             // Vitesse du scooter (réduite)
patrol_range = 250;  // Petite distance de patrouille
start_x = x;
direction_x = 1;     // 1 = droite | -1 = gauche
base_scale = image_xscale;
